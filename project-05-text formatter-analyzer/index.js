const strData =
  "    Welcome  to  the   JavaScript   World!!!   Learn,   Code,    Build.  ";
const removeSpace = strData.trim();

const removeSpecialCharector = removeSpace.replace(/[^\w\s]/g, "");
const trimstr = removeSpecialCharector.split(" ").filter(function (str) {
  if(trimstr 
    
  )
});

const totalStrCount = removeSpecialCharector.length;
console.log(removeSpecialCharector);
console.log(trimstr);
